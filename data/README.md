# Data

The observational FITS datasets used in this project are not included in the
repository due to their size.

Place the required FITS files in a local data directory before running the
analysis.
